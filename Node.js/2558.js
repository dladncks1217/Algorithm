var fs = require('fs');
var input = fs.readFileSync('/dev/stdin').toString().split('\n');
var i = parseInt(input[100]);
var trys = 0;
for(;trys<=i;trys++){
    var a = parseInt(input[0]).split(' ');
    console.log(a+b);
}